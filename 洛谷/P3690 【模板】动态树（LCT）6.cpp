#include <bits/stdc++.h>

using namespace std;
using i64 = long long;
const int N = 1e5 + 5;
int n, m;
int val[N];
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin >> n >> m;
    for (int i = 1; i <= n; i++)
        cin >> val[i];
    for(int i=1;i<=m;i++)
    {
        
    }
    return 0;
}